const isVowel = (character) => {
  if (
    character === "e" ||
    character === "a" ||
    character === "i" ||
    character === "o" ||
    character === "u" ||
    character === "E" ||
    character === "I"
  ) {
    return true;
  } else {
    return false;
  }
};
